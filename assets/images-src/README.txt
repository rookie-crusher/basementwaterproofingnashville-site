Drop original photos here (jpg/png, any size), then run: npm run images
They are converted to AVIF + WebP at 4 widths into public/images/ and
recorded in src/data/image-manifest.json so SmartImage can emit width/height.
